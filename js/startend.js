// confetti.start();

//   console.log("start");
// const start = () => {
//   setTimeout(function () {
//     confetti.start();
//   }, 1000);
// };

// const end = () => {
//   setTimeout(function () {
//     confetti.stop();
//   }, 2000);
// };

// start();
// end();

// console.log("getTotalLength", triangle.getTotalLength());
//
// console.log(document.getElementById("shape").getTotalLength());
// var list = document.querySelectorAll("path");
// console.log(list);
// console.log(list);
// var length = list.getTotalLength();
// console.log(length);
// list.style.strokeDasharray = length;
// list.style.strokeDashoffset = length;

// window.addEventListener("scroll", myFunction);

// function myFunction() {
//   var scrollpercent =
//     (document.body.scrollTop + document.documentElement.scrollTop) /
//     (document.documentElement.scrollHeight -
//       document.documentElement.clientHeight);

//   var draw = length * scrollpercent;

//   // Reverse the drawing (when scrolling upwards)
//   list.style.strokeDashoffset = length - draw;
// }
// list.style.strokeDashoffset = "0";

// window.onload = function () {
//   animatePath("#fi", "stroke-dashoffset 0.6s ease-in-out");
//   animatePath("#se", "stroke-dashoffset 0.5s 0.5s ease-in-out");
// };
// for (var i = 0; i < list.length; i++) {
//   var path = list[i],
//     length = path.getTotalLength();

//   path.style.strokeDasharray = length;
//   path.style.strokeDashoffset = length;
// }
// console.log(list);

// list.setAttribute("stroke-dashoffset", 0);
// list.style.setAttribute("stroke-dashoffset", 0);
